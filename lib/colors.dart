//coloress
import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor =
      Color(0xFFFFA500); // Naranja brillante (más parecido al de Amazon)
  static const Color accentColor =
      Color(0xFFFF8C00); // Naranja más oscuro para acentos
  static const Color backgroundColor =
      Color(0xFFFFFFFF); // Blanco para el fondo
  static const Color textColor = Color(0xFF333333); // Gris oscuro para texto
  static const Color iconColor =
      Color(0xFFFFA500); // Mismo naranja que primaryColor
  static const Color appBarColor = Color(0xFFFFA500); // Mismo que primaryColor
}
